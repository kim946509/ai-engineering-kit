# Contributing

실제 프로젝트에서 반복 사용해 효과를 확인한 구성요소를 우선합니다.

## 새 스킬

1. `skills/<kebab-case-name>/SKILL.md`를 만든다.
2. `name`과 폴더 이름을 일치시킨다.
3. 상세 계약은 `references/`, 생성할 파일은 `assets/`, 결정적 동작은 `scripts/`에 둔다.
4. 사람이 읽을 구조·사용법·예시는 `examples/<skill-name>/`에 둔다.
5. 외부 스킬 의존성은 해당 스킬 내부 preset에 출처와 고정 ref를 기록한다.
6. 저장소 검증과 실제 호출 평가를 통과시킨다.

## 새 플러그인

실제로 배포할 플러그인이 생겼을 때만 `plugins/<plugin-name>/`을 만든다. 스킬 저장소의 루트 manifest로 여러 컴포넌트를 하나의 플러그인처럼 보이게 하지 않는다.

## 확인사항

- 제품명, 사용자 경로, 자격증명 등 개인 정보가 템플릿에 남아 있지 않은가
- 자동 검증 없이 완료를 주장하지 않는가
- 새로운 자동화에 중단 조건과 롤백이 있는가
- README가 실제 설치 가능한 스킬이나 플러그인만 제공한다고 표현하는가
