# Contributing

이 저장소는 실제 프로젝트에서 반복 사용해 효과를 확인한 구성요소를 우선합니다.

## 새 스킬

1. `skills/<kebab-case-name>/SKILL.md`를 만든다.
2. `name`과 폴더 이름을 일치시킨다.
3. 반복적으로 필요한 결정적 동작만 `scripts/`에 둔다.
4. 상세 지식은 `references/`, 생성할 템플릿은 `assets/`에 둔다.
5. Codex metadata가 필요하면 `agents/openai.yaml`을 추가한다.
6. `catalog.json`에 실제 지원 상태를 기록한다.
7. 저장소 검증과 실제 호출 평가를 통과시킨다.

## 새 프로필

프로필은 generic core를 복제하지 않는 선택적 overlay입니다. 플랫폼의 실제 도구, 검증 명령, 승인 경계와 활성화 조건을 문서화하고, 미설치 도구를 사용할 수 있다고 가정하지 않습니다.

## 외부 프로젝트

외부 스킬이나 템플릿을 연결할 때는 원본 저장소, 고정 ref, 경로와 라이선스를 기록합니다. 코드를 vendoring해야 한다면 먼저 재배포 조건을 확인하고 attribution을 보존합니다.

## Pull request 확인사항

- 제품명, 사용자 경로, 자격증명 등 개인 정보가 템플릿에 남아 있지 않은가
- 자동 검증 없이 완료를 주장하지 않는가
- 새로운 자동화에 중단 조건과 롤백이 있는가
- 호환성 표기가 실제 검증 결과와 일치하는가
